# P1 HD-2D 전환 — HQ 검수 기록

> 검수: HQ 세션 / 2026-08-27
> 대상: `claude/p1-hd2d` (커밋 4ac9c55), `docs/reports/P1_report.md`
> 판정: **조건부 통과** — 구현·리포트 품질 우수. 잔여 3건: ① 판정 영역 밴드 A/B(r2 개정 미반영분) 보완 라운드, ② 실기기 검증, ③ 게이지 A안 최종 확정(사용자).

## 1. HQ 직접 검증

| 항목 | 결과 |
|---|---|
| `npm ci` + `npm run build` | 통과. gzip 136.2KB — 리포트와 일치 |
| Playwright 22종 | **22/22 통과** (1.4분, 재플레이크 없음 — M1 플레이크의 원인 규명·제거 확인) |
| 로직-렌더링 분리 | `src/core/` three.js import 0건. `field.ts`가 projection.ts를 순수 TS로 대체 — 구조 양호 |
| 게이지 A/B | `tests/gauge.spec.ts` 4조합 자동 실측 확인. **A안(배율 미적용)이 양 플레이 스타일에서 도약 1회·W4 도달로 기획 11.3과 정합** — HQ 권장안을 데이터가 지지 |
| r2 개정(판정 영역 A/B) | **미반영** — 릴레이가 빌드 마감 후 도착한 타이밍 문제. 보완 라운드로 지시 (3장) |

## 2. 질문 8건에 대한 HQ 결정

| # | 질문 | 결정 |
|---|---|---|
| 1 | 링 "화면 폭 60~70%" 기준 | **지름 기준 승인** (반경 기준은 화면 이탈 — 합리적 해석). 기획서 문언을 "지름 60~70%"로 개정 예정 |
| 2 | 게이지 배율 | **A안(미적용) 채택 권고 — 사용자 최종 확정 대기.** 패널 토글은 실기기 체감용으로 유지 |
| 3 | a-3 편대 클램프 | **승인.** "긴 스와이프 1회 다수 격파"는 전원이 판정 창을 지나야 성립. 유령 개체 방지 판단 타당 |
| 4 | 링 빗나간 통과형 무피해 | **승인** (기획서 문언 정합). 단 밴드 방식에서는 빗나감 자체가 구조적으로 감소 — A/B 비교 관찰 포인트로 지정 |
| 5 | 소녀 세로 위치 | 42% 유지 승인, 실기기 튜닝 대상 |
| 6 | 도약 화면 클램프 | 승인 |
| 7 | 링 내 동시 8기 | 상한 4기 유지. 최종 웨이브 조성 조정은 실기기 평가 후 P2에서 판단 |
| 8 | 픽셀 스케일링 | AD 스타일 확정 대기 유지, 기본 native |

편차 1~9: 전부 승인 (4번 "궤적은 과장, 판정은 실경로" 원칙은 우수 — 이후 이펙트 확장에도 유지할 것).

## 3. 보완 라운드 지시 (P1-r2 잔여분)

`docs/instructions/P1_hd2d_conversion.md` "구현 범위 2 (r2 개정)" 그대로: 화면 밴드 방식 추가 구현 + 패널 토글 + A/B 비교 실측(히트 성공률, 좌우 진입 대응, 빗나감 발생률, 시각 혼잡도)을 리포트에 추가. 완료 후 같은 브랜치에 푸시.

## 3-r2. 보완 라운드 검수 (2026-08-27, 커밋 801feb3)

- HQ 직접 검증: 빌드 통과, **Playwright 27/27** (판정 영역 신규 5종 포함), `src/core/` three.js import 0건 유지. `judgeArea.ts` 전략 패턴 격리 구조 양호.
- A/B 실측 품질 우수 — 통제 프로브(각도별 12기)·입력 정책 3종으로 비교 오염을 제거한 측정 설계, 기하 이론값 교차 검증 포함.
- **핵심 트레이드오프 (실기기 판단 재료):** 원형은 정하방 궤적의 34.8%가 구조적으로 링을 빗나가고(사선 22.9%), 밴드는 빗나감 0%. 대신 밴드는 점등이 세로 ±0.33 범위로 퍼져 "언제"는 명확하나 "어디를 그을지"는 플레이어 부담(무조준 히트율 19.4% vs 원형 44.4%, 점등 반응 시 75.0% vs 83.3%).
- 부수 발견 승인: a-2 청록과 하이라이트 민트의 색 근접 — AD 스타일 확정 시 하이라이트를 색상 아닌 명도·외곽선으로 구분할 것 (AD-N1 이후 전달 사항으로 등재).
- 실기기 검증용 빌드를 아티팩트로 배포 (원형/밴드는 패널 토글) — 게이트의 남은 절반은 사용자 실기기 판정.

## 4. 브랜치 처리 권장

보완 라운드 완료 + 사용자 실기기 확인 후 `claude/p1-hd2d` → main PR·머지 권장. 게이지 A안 확정 시 기획서 8·14장 개정을 HQ 브랜치에서 함께 처리.
